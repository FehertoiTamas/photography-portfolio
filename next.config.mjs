/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['en', 'hu'], // Nyelvek: angol és magyar
    defaultLocale: 'en',   // Alapértelmezett nyelv
  },
};

export default nextConfig;
